name: Traceserver Release

on:
  push:
    branches:
      - rocpd_support

jobs:
  build-and-release:
    runs-on: ubuntu-22.04
    timeout-minutes: 120

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Set up Maven
      uses: stCarolas/setup-maven@07fbbe97d97ef44336b7382563d66743297e442f # v4.5
      with:
        maven-version: 3.9.5

    - name: Build Traceserver for all platforms
      uses: coactions/setup-xvfb@b6b4fcfb9f5a895edadc3bc76318fae0ac17c8b3
      with:
        run: >-
          mvn -B clean install

    - name: Create timestamp for release
      id: timestamp
      run: echo "timestamp=$(date +'%Y%m%d-%H%M')" >> $GITHUB_OUTPUT

    - name: Find built artifacts
      id: artifacts
      run: |
        # Find the built product archives
        PRODUCT_DIR="trace-server/org.eclipse.tracecompass.incubator.trace.server.product/target/products"
        if [ -d "$PRODUCT_DIR" ]; then
          echo "linux_artifact=$(find $PRODUCT_DIR -name '*linux*.tar.gz' | head -1)" >> $GITHUB_OUTPUT
          echo "macos_x86_64_artifact=$(find $PRODUCT_DIR -name '*macosx*x86_64*.tar.gz' | head -1)" >> $GITHUB_OUTPUT
          echo "macos_aarch64_artifact=$(find $PRODUCT_DIR -name '*macosx*aarch64*.tar.gz' | head -1)" >> $GITHUB_OUTPUT
          echo "windows_artifact=$(find $PRODUCT_DIR -name '*win32*.zip' | head -1)" >> $GITHUB_OUTPUT
        else
          echo "Product directory not found: $PRODUCT_DIR"
          exit 1
        fi

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: traceserver-${{ steps.timestamp.outputs.timestamp }}
        release_name: Traceserver Release ${{ steps.timestamp.outputs.timestamp }}
        body: |
          Trace Compass Server packages for Windows, macOS, and Linux
          
          Built from commit: ${{ github.sha }}
          
          ## Downloads
          - **Linux**: trace-compass-server-*-linux.gtk.x86_64.tar.gz
          - **macOS (Intel)**: trace-compass-server-*-macosx.cocoa.x86_64.tar.gz  
          - **macOS (Apple Silicon)**: trace-compass-server-*-macosx.cocoa.aarch64.tar.gz  
          - **Windows**: trace-compass-server-*-win32.win32.x86_64.zip
        draft: false
        prerelease: false

    - name: Upload Linux Release Asset
      if: steps.artifacts.outputs.linux_artifact != ''
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ${{ steps.artifacts.outputs.linux_artifact }}
        asset_name: trace-compass-server-${{ steps.timestamp.outputs.timestamp }}-linux.gtk.x86_64.tar.gz
        asset_content_type: application/gzip

    - name: Upload macOS (Intel) Release Asset
      if: steps.artifacts.outputs.macos_x86_64_artifact != ''
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ${{ steps.artifacts.outputs.macos_x86_64_artifact }}
        asset_name: trace-compass-server-${{ steps.timestamp.outputs.timestamp }}-macosx.cocoa.x86_64.tar.gz
        asset_content_type: application/gzip

    - name: Upload macOS (Apple Silicon) Release Asset
      if: steps.artifacts.outputs.macos_aarch64_artifact != ''
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ${{ steps.artifacts.outputs.macos_aarch64_artifact }}
        asset_name: trace-compass-server-${{ steps.timestamp.outputs.timestamp }}-macosx.cocoa.aarch64.tar.gz
        asset_content_type: application/gzip

    - name: Upload Windows Release Asset
      if: steps.artifacts.outputs.windows_artifact != ''
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ${{ steps.artifacts.outputs.windows_artifact }}
        asset_name: trace-compass-server-${{ steps.timestamp.outputs.timestamp }}-win32.win32.x86_64.zip
        asset_content_type: application/zip

    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: traceserver-packages-${{ steps.timestamp.outputs.timestamp }}
        path: |
          trace-server/org.eclipse.tracecompass.incubator.trace.server.product/target/products/*.tar.gz
          trace-server/org.eclipse.tracecompass.incubator.trace.server.product/target/products/*.zip
        retention-days: 30